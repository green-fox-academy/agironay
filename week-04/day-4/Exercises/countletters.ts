'use strict';

function makeDict(takeThis: string) {
    takeThis.map(function (item) {

        for (let i: number = 0; i <= takeThis.length, i++) {

        }

        takeThis.length
        return
    }
    )
}

